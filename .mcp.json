{
  "mcpServers": {
    "askpolly": {
      "type": "http",
      "url": "${ASKPOLLY_PLUGIN_MCP_URL:-https://app.askpolly.ai/api/mcp}",
      "headers": {
        "Authorization": "Bearer ${ASKPOLLY_API_TOKEN}"
      }
    }
  }
}
