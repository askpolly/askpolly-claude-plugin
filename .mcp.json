{
  "mcpServers": {
    "tools": {
      "type": "http",
      "url": "https://staging.askpolly.ai/api/mcp",
      "headers": {
        "Authorization": "Bearer ${ASKPOLLY_API_TOKEN}"
      }
    }
  }
}
