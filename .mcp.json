{
  "mcpServers": {
    "tools": {
      "type": "http",
      "url": "https://app.askpolly.ai/api/mcp",
      "headers": {
        "Authorization": "Bearer ${ASKPOLLY_API_TOKEN}"
      }
    }
  }
}
